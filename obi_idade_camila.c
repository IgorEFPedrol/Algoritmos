#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int idades[2], camila;

    printf("Insira 3 idades: \n");

    scanf("%d", &idades[0]);
    scanf("%d", &idades[1]);
    scanf("%d", &idades[2]);

    if (idades[0] > idades[1] && idades[0] > idades[2])
    {
        cibele
    }

    printf("\n%d", idades[]);

    return 0;
}
